import { SortCourseDirective } from './sort-course-directive';

describe('SortCourseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SortCourseDirective(null);
    expect(directive).toBeTruthy();
  });
});
