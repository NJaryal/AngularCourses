import { SortCourseDirectiveDirective } from './sort-course-directive.directive';

describe('SortCourseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SortCourseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
